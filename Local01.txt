cd -- change directory
ls -- list the contents inside the current folder.


git commands:
1. create a new project in github  -- halo


run commands to establish connections between server (github) and local
git init  (initialize your current folder as git local repository)

we made a new, then run the command below to add to git library.
git add .  (add new files to git library.  . means every new files)
git status (show current status of new files)

git commit -m "your comments"  (commit the new file to git)

git push (push local files to server)
git pull (get the lastet file from server)

Wechat
QQ
IG
